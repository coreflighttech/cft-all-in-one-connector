# CFT All-in-One Connector UI

This project contains the compact Windows interface for CFT All-in-One Connector.

The interface displays device, simulator, and aircraft status; sends native Run/Stop commands to the hidden CFT engine; and exposes engine output through an optional resizable Debug panel.

See the repository-level `README.md` for product scope, usage, validated hardware, architecture, and licensing information.

